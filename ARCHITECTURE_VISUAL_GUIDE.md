# Section Architecture - Visual Guide

## 🏗️ Architecture Overview

```
┌─────────────────────────────────────────────────────────────┐
│                         App.tsx                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  useDynamicHome flag controls which page to use        │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                            │
                ┌───────────┴───────────┐
                ▼                       ▼
    ┌────────────────────┐  ┌────────────────────┐
    │   Home.tsx         │  │  DynamicHome.tsx   │
    │   (Static)         │  │  (Dynamic)         │
    └────────────────────┘  └────────────────────┘
                                      │
                                      ▼
                          ┌────────────────────┐
                          │  sectionConfig.ts  │
                          │  ┌──────────────┐  │
                          │  │ Sections:    │  │
                          │  │ - Header     │  │
                          │  │ - Hero       │  │
                          │  │ - Services   │  │
                          │  │ - Pricing    │  │
                          │  │ - etc.       │  │
                          │  └──────────────┘  │
                          └────────────────────┘
                                      │
                                      ▼
                          ┌────────────────────┐
                          │ SectionRenderer    │
                          │  ┌──────────────┐  │
                          │  │componentMap  │  │
                          │  │- Header      │  │
                          │  │- Hero        │  │
                          │  │- Services    │  │
                          │  └──────────────┘  │
                          └────────────────────┘
                                      │
                                      ▼
                          ┌────────────────────┐
                          │ Actual Components  │
                          │  - Header.tsx      │
                          │  - Hero.tsx        │
                          │  - Services.tsx    │
                          └────────────────────┘
```

## 📊 Data Flow

```
┌─────────────────┐
│ Content Files   │
│ - heroContent   │
│ - services      │
│ - pricing       │
└────────┬────────┘
         │
         ▼
┌─────────────────┐      ┌──────────────┐
│ Section Config  │─────▶│  Component   │
│ - id            │      │              │
│ - component     │      │  Renders     │
│ - enabled       │      │  with props  │
│ - order         │      │              │
│ - props ────────┼─────▶│              │
└─────────────────┘      └──────────────┘
```

## 🎨 Theme System

```
┌──────────────────────────────────────────────────┐
│              themeVariants.ts                     │
│                                                   │
│  ┌─────────────┐  ┌─────────────┐               │
│  │  Business   │  │   Sales     │               │
│  │   Theme     │  │   Theme     │               │
│  │             │  │             │               │
│  │ ┌────────┐  │  │ ┌────────┐  │  ┌─────────┐ │
│  │ │ Hero   │  │  │ │ Hero   │  │  │ Minimal │ │
│  │ │Services│  │  │ │Pricing │  │  │  Theme  │ │
│  │ │Pricing │  │  │ │Services│  │  │         │ │
│  │ └────────┘  │  │ └────────┘  │  └─────────┘ │
│  └─────────────┘  └─────────────┘               │
└──────────────────────────────────────────────────┘
                    │
                    ▼
          ┌──────────────────┐
          │   ThemedHome     │
          │   (selects       │
          │    theme)        │
          └──────────────────┘
```

## 🛠️ Section Manager Flow

```
┌──────────────────────────────────────┐
│      User Opens Section Manager      │
└──────────────────────────────────────┘
                │
                ▼
┌──────────────────────────────────────┐
│   Shows List of All Sections         │
│   ┌────────────────────────────┐     │
│   │ ☑ Header          ↑ ↓     │     │
│   │ ☑ Hero            ↑ ↓     │     │
│   │ ☐ Services        ↑ ↓     │     │
│   │ ☑ Pricing         ↑ ↓     │     │
│   └────────────────────────────┘     │
└──────────────────────────────────────┘
                │
       ┌────────┼────────┐
       ▼        ▼        ▼
   Toggle   Reorder   Export
   Section   Section   Config
```

## 🔄 Section Lifecycle

```
1. Config Defined
   ↓
2. Enabled Check
   ↓
3. Sorted by Order
   ↓
4. Props Merged
   ↓
5. Component Mapped
   ↓
6. Rendered
   ↓
7. User Interaction
```

## 📱 Responsive Behavior

```
Desktop View              Mobile View
┌────────────────┐       ┌──────────┐
│ Header         │       │ Header   │
│ Hero (Full)    │       │ Hero     │
│ Services (2x2) │       │ Services │
│ Pricing (3col) │       │ (Stack)  │
│ Footer         │       │ Pricing  │
└────────────────┘       │ (Stack)  │
                         │ Footer   │
                         └──────────┘
```

## 🎯 Common Patterns

### Pattern 1: A/B Testing
```
User Group A → Business Theme → Sections 1-10
User Group B → Sales Theme    → Sections 1,3,2,4-10
                                (pricing moved up)
```

### Pattern 2: Campaign Landing Page
```
URL: /campaign/blackfriday
    ↓
Detect campaign
    ↓
Load campaignTheme
    ↓
Hide header/footer
Show only: Hero → Pricing → Contact
```

### Pattern 3: User Role Based
```
Anonymous User  → Show: Hero, Services, Pricing
Logged In User  → Show: Dashboard Link in Header
Admin User      → Show: Section Manager
```

## 🔍 File Relationships

```
App.tsx
  ├─ imports DynamicHome
  │
  └─ DynamicHome.tsx
       ├─ imports sectionConfig
       │    └─ sectionConfig.ts
       │         └─ defines all sections
       │
       ├─ imports SectionRenderer
       │    └─ SectionRenderer.tsx
       │         ├─ imports Header
       │         ├─ imports Hero
       │         ├─ imports Services
       │         └─ etc.
       │
       └─ optionally imports SectionManager
            └─ SectionManager.tsx
                 └─ visual editing UI
```

## ⚡ Performance Optimization

```
Lazy Loading Sections
┌──────────────────────┐
│ Hero (Eager Load)    │ ← Loads immediately
├──────────────────────┤
│ Services (Lazy)      │ ← Loads when visible
├──────────────────────┤
│ Pricing (Lazy)       │ ← Loads when scrolled
└──────────────────────┘
```

## 🎨 Customization Points

```
Section Config
    ├─ enabled: true/false
    ├─ order: 1-10
    └─ props: { custom: 'data' }
            │
            ▼
       Component receives props
            │
            ▼
       Renders with custom data
```

---

This visual guide helps understand how all pieces fit together!
